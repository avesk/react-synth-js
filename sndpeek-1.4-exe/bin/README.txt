OS X universal binary, 10.3.9 and up

to install (optional):
    > sudo cp sndpeek /usr/bin/
    (enter password)
    > sudo chmod 755 /usr/bin/sndpeek

to run:
    (from Terminal)
    > sndpeek

    (or if not installed)
    > ./sndpeek

it is also possible double click to run
